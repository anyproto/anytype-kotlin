package com.anytypeio.anytype.core_models.ui

/**
 * Category of MIME types for file classification.
 * Used for displaying appropriate icons for different file types.
 */
enum class MimeCategory {
    IMAGE, PDF, TEXT, AUDIO, VIDEO, ARCHIVE, OTHER, TABLE, PRESENTATION
}
