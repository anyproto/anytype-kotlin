<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="5dip" />
            <gradient
                android:startColor="@color/progressbar_background"
                android:centerColor="@color/progressbar_background"
                android:centerY="0.75"
                android:endColor="@color/progressbar_background"
                android:angle="270"
                />
        </shape>
    </item>

    <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape>
                <corners android:radius="5dip" />
                <gradient
                    android:startColor="@color/progressbar_secondary_progress"
                    android:centerColor="@color/progressbar_secondary_progress"
                    android:centerY="0.75"
                    android:endColor="@color/progressbar_secondary_progress"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>
    <item
        android:id="@android:id/progress"
        >
        <clip>
            <shape>
                <corners
                    android:radius="5dip" />
                <gradient
                    android:startColor="@color/progressbar_secondary_progress"
                    android:endColor="@color/progressbar_secondary_progress"
                    android:angle="270" />
            </shape>
        </clip>
    </item>

</layer-list>