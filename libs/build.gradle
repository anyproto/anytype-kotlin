plugins {
    id 'maven-publish'
}

publishing {
    publications {
        create("myLocalPublication", MavenPublication) {
            artifact(file('lib.aar'))
            groupId = 'io.anyproto'
            artifactId = 'anytype-heart-android'
            version = '0.99'
        }
    }
}
