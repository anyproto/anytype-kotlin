package com.anytypeio.anytype.presentation.confgs

import com.anytypeio.anytype.core_models.Id
import com.anytypeio.anytype.presentation.BuildConfig

object ChatConfig {

    const val MAX_ATTACHMENT_COUNT = 10
    const val MAX_USER_REACTION_COUNT = 3
    const val MAX_REACTION_COUNT = 12

    const val MAX_MESSAGE_CHARACTER_LIMIT = 4000
    const val MAX_MESSAGE_CHARACTER_OFFSET_LIMIT = 3950

    const val GROUPING_DATE_INTERVAL_IN_SECONDS = 60
}